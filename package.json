{
  "name": "theta-gateway",
  "version": "3.9.0",
  "description": "SSH jump host for the theta42 stack",
  "scripts": {
    "test": "npm --prefix nodejs test",
    "start": "npm --prefix nodejs start"
  }
}
